# 開発（ローカルLLM）⇔ 本番（ChatGPT-API）の切替手順

このバンドルは **コード無改修** のまま、設定ファイルだけで開発／本番を切り替えられる。
現在は「開発（ローカルLLM）」構成。**アプリの .py は本番と完全に同一。**

## 現在の開発構成（2026-08-30 設定）

| 項目 | 値 |
|---|---|
| LLM（チャット・裏方・LightRAG 全部） | Ollama gpt-oss:20b（tools対応） |
| 裏方用の別名 | `gpt-5.6-sol`（`ollama cp` による別名。実体は gpt-oss:20b） |
| 埋め込み | bge-m3（1024次元）・索引は `LightRAG_servers/rag_storage_local/` |
| 本番用の索引 | `LightRAG_servers/rag_storage/`（OpenAI 1536次元）**無傷で温存中** |
| Ollama 環境変数（ユーザー） | `OLLAMA_CONTEXT_LENGTH=65536` / `OLLAMA_KV_CACHE_TYPE=q8_0` |

## 本番（ChatGPT-API）へ戻す手順

1. **アプリの設定を復元**
   `aiagent_minimal_rag_tougou\env.openai.bak` → `env` に上書きコピー
   （api.openai.com + 本物のAPIキー。キーが失効していれば新しいキーに差し替え）

2. **LightRAG の設定を復元**
   `LightRAG_servers\.env.openai.bak` → `.env` に上書きコピー
   （LLM=gpt-4.1-nano / 埋め込み=text-embedding-3-small 1536 / WORKING_DIR=./rag_storage）
   ※ 索引の再構築は**不要**。rag_storage をそのまま読む。

3. **モデル設定を書き戻す（削除ではなく上書き！）**
   管理画面「モデル設定」で 候補と既定を OpenAI のモデル名（例 gpt-5.6-sol）にして保存。
   `data/model_settings.yaml` を**削除だけで済ませないこと**。開発中に誰かが選んだ
   ローカルモデル名が「利用中モデル」経由でプルダウンに残り、OpenAI に存在しない
   モデルを要求してエラーになる罠がある（core.py の available() の仕様）。

4. **Ollama の別名を掃除（任意）**
   `ollama rm gpt-5.6-sol`（qwen2.5:7b 本体は残る）。環境変数は残しても無害。

5. **再起動**
   LightRAG: `stop-servers.ps1` → `start-servers.ps1`（21台）
   アプリ: 停止 → `.venv\Scripts\python.exe run.py`

6. **確認**
   `check-servers.ps1` で 21台 healthy・キーOK・文書78件 →
   チャットで「8月の装置別MTTR」（期待値: C装置63/D装置55/A装置47/B装置18分）と
   「A装置のセンサー交換の締付トルクは？」（期待値: 4.5N・m、出典つき）。

## 開発（ローカル）へ再度切り替える手順

上記の逆。`env` / `.env` を本手順書冒頭の開発構成に編集
（`.env` は LLM_MODEL=gpt-oss:20b, LLM_BINDING_HOST=http://127.0.0.1:11434/v1,
LLM_BINDING_API_KEY=ollama, WORKING_DIR=./rag_storage_local,
EMBEDDING_MODEL=bge-m3, EMBEDDING_DIM=1024, EMBEDDING_BINDING_HOST=同上, KEY=ollama）、
`ollama cp gpt-oss:20b gpt-5.6-sol`、model_settings.yaml を gpt-oss:20b に、各再起動。

## DB統一（2026-08-30 実施・モデル切替とは独立）

- 21個のDBを **`data/統合.db` 1つに統一**した。表名は「元DB名__表名」
  （例: `equipment.db` の `stop_records` → `equipment__stop_records`）。114表・38,970行。
- 移行時に**新旧の等価検証 233本すべて一致**を確認済み。カタログ（meta.yaml）・
  例文105件・検算・関係線・ER配置も機械変換して引き継いだ。
- カタログが1モデルの文脈に収まらないときは**表ルーター**（route_tables）が
  質問に関係する表だけに絞り、関係線で結合相手を補完する（SCOPE_MODE=auto の正規分岐）。
- **巻き戻し**: 旧21DBは `data/_multi_backup/` に無傷で退避してある。
  `_multi_backup/migrate_single_db.py rollback` を実行（または統合.dbと
  統合.db.meta.yaml を消して _multi_backup の中身を data/ に戻す）で5分で復元できる。
- この構成は開発・本番共通。**本番切替でDBを触る必要はない。**

## 知っておくべき挙動差（開発≠本番）

- カタログが選択中モデルの読める量に収まれば全スキーマ直載せ、収まらなければ
  表ルーターで絞る（SCOPE_MODE=auto）。本番（大きい文脈）は直載せ、
  開発（64K）は絞る経路を通ることが多い。どちらもアプリ正規の分岐。
- 開発はモデル品質・RAG抽出品質が本番より低く、画像添付（vision）は無効。
- したがって開発で確認するのは**機能とフロー**。回答品質の最終確認は本番構成で行うこと。

## 注意（環境非依存の恒久修正）

- `LightRAG_servers/*.ps1` には UTF-8 **BOM を付加済み**（PowerShell 5.1 が BOM 無しを
  Shift-JIS と誤認して構文エラーになるため）。別PCへ移す際もこのまま持っていくこと。
- 埋め込み設定の `EMBEDDING_BINDING_HOST` 行は**絶対に削除しない**こと。
  未設定だと LLM_BINDING_HOST を継承する仕様のため、静かに接続先が入れ替わる。
