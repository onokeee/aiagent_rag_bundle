flask>=3.0
waitress>=3.0          # 本番用WSGIサーバ（Windows/Linuxどちらでも動く）
openai>=1.40
python-dotenv>=1.0
requests>=2.31         # ナレッジベース(LightRAGサーバ)へのHTTP呼び出し
#   入っていない環境でも起動はする。ナレッジ検索を使おうとしたときだけ止まる。
pandas>=2.0
plotly>=5.20           # グラフ。JSはパッケージ同梱のものを配信する（CDN不要）
PyYAML>=6.0
openpyxl>=3.1          # Excelの読み書き
scipy>=1.11            # 検定・分布・クラスタリング
statsmodels>=0.14      # 回帰・指数平滑・ARIMA・季節分解
python-pptx>=1.0       # PowerPointレポート（グラフは画像でなくネイティブ形式）
# メール送信は標準ライブラリの smtplib を使うので追加インストールは不要

python-docx>=1.1        # Word（図表つきの報告書）
kaleido>=0.2            # グラフを画像にする（Wordに貼るのに必要）
#   kaleido は裏で Chrome を使う。Linuxサーバでは chromium を入れておくこと。
#   入っていない環境でも文書は作れる（グラフが表に置き換わるだけ）。
