# 別のPCへ移す手順

このzipには、アプリ・ナレッジベースの文書と索引・データカタログ・DBが
すべて入っています。**作り直しは不要**で、APIキーの設定と依存関係の導入だけで動きます。

    aiagent_minimal_rag_tougou/   アプリ本体
      data/                       DB 21個・カタログ21件・KB登録簿・会話履歴
    LightRAG_servers/             ナレッジベースのサーバ
      docs/                       文書 78件（21のナレッジベース）
      rag_storage/                索引 21ワークスペース（作り直し不要＝費用ゼロ）

---

## 1. 置く場所

どこでも構いませんが、**2つのフォルダは同じ階層に置いてください**。
以下は デスクトップ に置いた例です。

    C:\Users\<あなた>\Desktop\aiagent_minimal_rag_tougou
    C:\Users\<あなた>\Desktop\LightRAG_servers

## 2. Python を用意する

| 用途 | 必要なバージョン |
|---|---|
| アプリ | 3.9 以上 |
| LightRAG サーバ | **3.10 以上**（3.13 で動作確認済み） |

両方を1つのPythonでまかなうなら 3.13 を入れてください。

## 3. アプリ側の準備

```
cd aiagent_minimal_rag_tougou
py -3.13 -m venv .venv
.venv\Scripts\python.exe -m pip install -r requirements.txt
```

**設定ファイル `env` を作ります。** `env.example` を `env` という名前でコピーし、
次の2行を書いてください（APIキーは移行元のPCの `env` からコピーするか、
OpenAIの管理画面で新しく発行してください）。

```
export OPENAI_BASE_URL="https://api.openai.com/v1"
export OPENAI_API_KEY="sk-..."
```

## 4. LightRAG サーバ側の準備

```
cd LightRAG_servers
py -3.13 -m venv venv
venv\Scripts\python.exe -m pip install "lightrag-hku[api]"
```

**設定ファイル `.env` を作ります。**（アプリ側の `env` とは別物で、
こちらはドット付き。LightRAG本体がこの名前しか読まないためです）
`env.sample` を `.env` という名前でコピーし、APIキーの2行を埋めてください。

## 5. 起動

```
# ナレッジベースのサーバ（21台）
cd LightRAG_servers
powershell -ExecutionPolicy Bypass -File start-servers.ps1

# 状態の確認（healthy / キー有効 / 文書件数）
powershell -ExecutionPolicy Bypass -File check-servers.ps1

# アプリ
cd ..\aiagent_minimal_rag_tougou
.venv\Scripts\python.exe run.py
```

ブラウザで `http://127.0.0.1:8000` を開き、`admin` / `adminpass` でログインします。

## 6. 動作確認

- サイドバーに「社内文書の検索先」が **21件** 出ていること
- 「データカタログ」に **21個のDB** が出ていること
- チャットで「8月の装置別MTTRを教えて」と聞いて表が返ること
- 「A装置のセンサー交換の締付トルクは？」で出典つきの回答が返ること

---

## 注意

### メモリ
LightRAG 21台で約1.2GB使います。**物理メモリ8GB以上**を推奨します。
足りない場合は `start-servers.ps1` の `$servers` から不要な行を消すか、
アプリの「ナレッジベース」画面で使わないものを無効にしてください。

### APIキーは入っていません
`env` と `.env` は、キーが入るためこのzipから除いてあります。
移行元のPCからコピーするか、新しく発行してください。

### 索引は作り直し不要
`rag_storage/` をそのまま持ってきているので、`ingest.py` の実行は不要です
（実行すると「済み」として飛ばされます）。文書を足したときだけ実行してください。

### 会話履歴について
`data/users/` に移行元での会話が入っています。不要ならフォルダごと消して構いません
（アプリが自動で作り直します）。
